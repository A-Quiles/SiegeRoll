import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gg.siegeroll.app',
  appName: 'SiegeRoll',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
